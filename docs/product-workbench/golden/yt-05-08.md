# Golden Journey Record — YT-05 → YT-08

Status: **Golden**

Human-approved on: **2026-09-10**

Frozen executable candidate:

`d5309a96d532ee107011c2a5cefc3000b9e4932f`

This record binds product truth to the exact executable above. Later docs-only descendants do not redefine the executable candidate. Any later executable UX change requires a new candidate → independent review → explicit human approval cycle.

## Why this executable is Golden

Independent Product Reviewer #34 classified the exact executable `GOLDEN-READY` after direct five-lens review and direct inspection of the rendered evidence.

Exact-head evidence:
- ETHOnline Continuity Gate `34497826067`: **SUCCESS**;
- Product Workbench Visual Check `34497819955`: **SUCCESS**;
- rendered artifact `product-workbench-rendered-evidence` / `10160672183`;
- **44 PNGs**: 22 meaningful checkpoints at desktop `1440×1000` and the same 22 at mobile `390×844`;
- five-lens review: Product/interaction PASS; Visual/brand PASS; Accessibility PASS at the review boundary; Copy/comprehension PASS; Trust/authority PASS;
- Devinson explicitly approved this exact executable for freeze on 2026-09-10.

## Frozen journey

`secure approval → Ledger not ready → waiting → approve / reject / cancel → Recovery active → exact human-backed delegated agent → 32 USDC blocked → optional lower-minimum replacement-authority seam → replacement reject/cancel preserves current 40 USDC recovery → 45 USDC inside scope → transfer + settlement completion state → You recovered 45 USDC → Friday Yoga no longer usable by Maya`

## Frozen product behavior

### YT-05 — Delegate
- authority remains booking-scoped to Friday Yoga;
- minimum recovery remains 40 USDC;
- expiry remains `Tomorrow · 17:00`;
- cancellation remains forbidden;
- initial reject/cancel creates no recovery authority;
- replacement authorization does not silently widen the active mandate;
- proposed 30-USDC replacement remains separate from the active 40-USDC mandate until approved;
- rejecting/cancelling the replacement leaves the existing 40-USDC recovery active and unchanged;
- Ledger is presented as authorizing the off-chain Recovery Mandate, not signing Hedera HTS transactions.

### YT-06 — Agent working
- customer sees `Recovery active`;
- exact delegated agent is shown as verified and human-backed;
- no raw World human identifier appears in customer UI;
- active 40-USDC minimum, expiry, Friday-Yoga-only scope and no-cancel/no-widen limits remain visible;
- customer has a clear `Stop recovery` action.

### YT-07 — Block / escalate
- a 32-USDC offer is visibly blocked;
- no booking transfer or settlement occurs for the blocked offer;
- customer is not needlessly interrupted by the bad offer;
- lowering the minimum requires a fresh authorization decision;
- existing authority remains active unless a replacement mandate is successfully approved.

### YT-08 — Successful recovery
- a 45-USDC offer is visibly inside the approved mandate;
- the agent proceeds without another owner approval prompt;
- completion reads `You recovered 45 USDC`;
- Friday Yoga becomes transferred/recovered and is no longer shown as Maya's usable booking;
- technical proof remains secondary to the customer result.

## Truth boundary

This Golden record freezes the **product behavior and presentation contract**. It does not promote fixture sponsor transitions to LIVE evidence.

The UX candidate currently models sponsor-dependent transitions as fixture/demo state where real integration is not yet wired. Integration must replace those fixtures with real sponsor-backed state without changing the Golden customer journey silently.

Sponsor ownership remains:
- **Ledger** — real device-backed authorization/rejection/cancellation of the Recovery Mandate;
- **World** — human-backed requester verification + exact delegated-agent binding;
- **Hedera** — booking authority, transfer, settlement and public proof.

## Next product gate

Do not jump directly into a holder-only YT-09/YT-10 extension.

Before the next complete product slice:
1. extract the YourTurn `DESIGN.md` / design contract from approved Golden evidence;
2. establish the domain glossary and craft/review rules;
3. apply `stakeholder-coverage-gate.md` and issue #39;
4. map the acquirer and provider lanes;
5. choose the smallest connected next slice that respects:

`provider rules ∩ holder mandate ∩ acquirer eligibility/payment`

Before this Golden holder flow is treated as integrated product truth, maintain a Golden-to-integration acceptance ledger that maps every fixture state/action to its real implementation owner, evidence, and failure behavior.