# World verifier foundation qualification

This bounded package/source increment starts from canonical projection `1201ae68a0d33b8dbf661346425ada21518e04b8`, independently cleared in #16 comment 5647912153. Claim: #5 comment 5648009027. It adds exact `@worldcoin/agentkit` 0.2.1 without changing inherited security overrides or any inherited lock entry's version, resolved URL or integrity. Five dependency entries are added; the focused check pins the complete inherited graph digest.

The three `lib/world-agentkit/{server-verifier,trust-boundary,nonce-store}.ts` files and `scripts/world-agent-contract-check.mjs` are byte-identical to qualified World source `355f8c45fdd3024bf1407c0f25f27406ccbd8bb0`. The focused test pins their SHA-256 values. No old ApprovalGrant write gate, saga, HTTP route, live runner or additive MCP tool is copied.

`verifyWorldAgentRequest` consumes a signed header plus independently selected exact requester/resource, injected nonce store and optional AgentBook lookup. Its official parser, message validation, signature verification and AgentBook lookup produce only a human-backed requester signal. Full resource equality and exact expected requester checks precede one-shot nonce consumption. Consumers must independently resolve and revalidate current Ledger authority, owner/agent enrollment, provider policy and Hedera state. This package does not implement that composition or an execution route.

Qualification uses public deterministic test-only keys, local signatures, injected AgentBook results and an atomic in-memory nonce model. The existing Redis nonce implementation is retained byte-for-byte and typechecked; these tests do not assert external Upstash connectivity or production nonce runtime evidence. Existing Ledger current/operation scripts run separately against disposable real Redis in CI. The new compatibility suite additionally covers actual installed official parser/signature recovery, 12-way duplicate concurrency, malformed headers, expired/aged messages, exact resource binding, AgentBook/storage failures, response minimization and unchanged graph/source checks.

This is source/dependency compatibility evidence only. Real AgentBook registration, signed HTTP route evidence, physical Ledger provenance, Bob-funded Hedera settlement and independent integration acceptance remain separate. The default AgentBook path is never invoked during qualification. No deployment configuration, enrollment records or credentials are created/read. A future caller must use the actual verified requester and operation-owned current source; it must not turn an odd authority version into an even version.
