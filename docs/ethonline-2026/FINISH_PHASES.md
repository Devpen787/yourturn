# ETHOnline 2026 Finish Phases

This file is a release/package router, not an implementation backlog.

## Phase 1 — Product truth — COMPLETE

Human-approved Product R5 FIXTURE: `342f46ee6e0c4d0f332287d8433735c5ac015528`.

Do not modify its bytes during finish-line packaging.

## Phase 2 — Component/runtime qualification — COMPLETE at bounded software scope

- Frozen runtime ancestor `25bc842b...`: independently bounded-cleared SOURCE/CI/LOCAL.
- Finish-line post-confirm + one-shot controls: exact `60a51fe09e735409a1c0b35593bc4413a49016e1`, run `34727782318`, independently CLEARED SOURCE / hosted CI / LOCAL shape under #16 `5649737536`.

This is not LIVE transport/DEVICE/credential execution.

## Phase 3 — Final software selection — COMPLETE / FROZEN

Issue #5 `5650011760` selected exact final software `60a51fe09e735409a1c0b35593bc4413a49016e1`, tree `f60a892ce6ecdb7ebb22b49e0e23985f938b7205`. Freeze is active; no runtime successor is justified absent a concrete final-Security REVISE or actually missing implementation.

`feature/finishline-execution` may still point to ancestor `00a9494866191628e243cc0301792d428039978a`. Ref movement is not required to preserve the selected exact bytes.

## Phase 4 — Whole-candidate Security — COMPLETE at SOURCE / HOSTED-CI / LOCAL

Issue #16 `5650032923` CLEARED the exact selected software candidate with no CRITICAL/HIGH composition defect.

Claim boundary remains: no LIVE/testnet/device/credential execution, no automatic Hedera signing/submission, and no single LIVE Ledger→World→Hedera run.

## Phase 5 — Repository/submission package — DOCS-ONLY PACKAGE PREPARED

The selected-SHA + whole-candidate-Security trigger is met. Package Gate has prepared a docs-only successor on the selected software tree, preserving runtime/Product/package/lock/workflow executable bytes. No branch/ref/default/deploy/submission action is implied by that commit object.

Package synchronization refreshes README, feedback discoverability, FINAL_EVIDENCE, ACCEPTANCE, Reviewer Guide, current-state/phase routing, llms/AGENTS routing, media state, Evidence Index routing and stale demo/economics claims.

## Phase 6 — Exact-final browser / cold judge — WAITING ON PACKAGE + RUN

No exact-final browser/Playwright run exists yet for `60a51fe09e735409a1c0b35593bc4413a49016e1`. Qualification `34727782318` is green but is not browser evidence. Run the credential-free browser workflow through an authorized non-deploying path on the exact frozen executable bytes or the docs-only package successor, then cold-judge the real Source Code entry.

## Phase 7 — Source-code URL / final submission lock — WAITING

Final Source Code must route judges to the assembled ETHOnline package (a safe `/tree/<release-ref>` or exact packaged permalink), not the stale bare-repo default Week-5 entry. Repo description/default discovery remain release-gate actions.

## Optional final-path proof upgrade

A new combined Ledger→World→Hedera ceremony may be run if time/risk justify it. It is an evidence upgrade for the exact final integrated path; it does not erase or invalidate settled historical sponsor evidence/video.
